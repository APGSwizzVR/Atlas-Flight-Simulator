const $=s=>document.querySelector(s);const config=window.ATLAS_CONFIG||{};const modal=$("#modal"),label=$("#modal-label"),title=$("#modal-title"),body=$("#modal-text");
$("#year").textContent=new Date().getFullYear();
function action(kind){const url=kind==="discord"?config.DISCORD_URL:config.COFFEE_URL;if(url){window.open(url,"_blank","noopener,noreferrer");return}label.textContent=kind==="discord"?"ATLAS COMMUNITY":"SUPPORT ATLAS";title.textContent=kind==="discord"?"Discord is coming soon.":"Support is coming soon.";body.textContent=kind==="discord"?"The official Atlas FS Discord link has not been configured yet. Atlas is still at the beginning of development.":"The Buy Me a Coffee link has not been configured yet. This option will be enabled when the support page is ready.";modal.classList.add("open");modal.setAttribute("aria-hidden","false")}
document.querySelectorAll("[data-action]").forEach(el=>el.addEventListener("click",()=>action(el.dataset.action)));
document.querySelectorAll("[data-close]").forEach(el=>el.addEventListener("click",()=>{modal.classList.remove("open");modal.setAttribute("aria-hidden","true")}));
addEventListener("keydown",e=>{if(e.key==="Escape"){modal.classList.remove("open");modal.setAttribute("aria-hidden","true")}});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>observer.observe(e));
const glow=$(".cursor-glow");addEventListener("pointermove",e=>{glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"},{passive:true});
const track=$("#aircraftTrack");let down=false,start=0,scroll=0;track.addEventListener("pointerdown",e=>{down=true;start=e.clientX;scroll=track.scrollLeft;track.setPointerCapture(e.pointerId)});track.addEventListener("pointermove",e=>{if(down)track.scrollLeft=scroll-(e.clientX-start)*1.1});["pointerup","pointercancel","pointerleave"].forEach(n=>track.addEventListener(n,()=>down=false));
document.querySelectorAll(".aircraft-card").forEach(card=>{card.addEventListener("pointermove",e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform="perspective(1000px) rotateX("+(-y*2)+"deg) rotateY("+(x*2)+"deg) translateY(-4px)"});card.addEventListener("pointerleave",()=>card.style.transform="")});
