const earth=document.getElementById("earth"),worldCanvas=document.getElementById("worldCanvas");const canvases=[earth,worldCanvas].filter(Boolean);
function globe(canvas,detail=1000){const ctx=canvas.getContext("2d");let dpr=devicePixelRatio||1,rot=0,target=0,drag=false,lastX=0;const dots=[];for(let i=0;i<detail;i++){const z=Math.random()*2-1,a=Math.random()*Math.PI*2,s=Math.sqrt(1-z*z);dots.push({x:s*Math.cos(a),y:z,z:s*Math.sin(a)})}
function resize(){const r=canvas.getBoundingClientRect();canvas.width=r.width*dpr;canvas.height=r.height*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}resize();addEventListener("resize",resize);
canvas.addEventListener("pointerdown",e=>{drag=true;lastX=e.clientX;canvas.setPointerCapture(e.pointerId)});canvas.addEventListener("pointermove",e=>{if(drag){target+=(e.clientX-lastX)*.006;lastX=e.clientX}});canvas.addEventListener("pointerup",()=>drag=false);canvas.addEventListener("pointercancel",()=>drag=false);
function draw(){const w=canvas.clientWidth,h=canvas.clientHeight,cx=w/2,cy=h/2,r=Math.min(w,h)*.42;ctx.clearRect(0,0,w,h);const g=ctx.createRadialGradient(cx-r*.28,cy-r*.32,0,cx,cy,r*1.2);g.addColorStop(0,"rgba(87,176,239,.42)");g.addColorStop(.48,"rgba(22,71,111,.55)");g.addColorStop(1,"rgba(2,8,14,0)");ctx.fillStyle=g;ctx.beginPath();ctx.arc(cx,cy,r*1.08,0,Math.PI*2);ctx.fill();dots.forEach(p=>{const x=p.x*Math.cos(rot)-p.z*Math.sin(rot),z=p.x*Math.sin(rot)+p.z*Math.cos(rot);if(z<-.08)return;const y=p.y,px=cx+x*r,py=cy+y*r;ctx.fillStyle="rgba(179,224,255,"+(.1+z*.65)+")";ctx.beginPath();ctx.arc(px,py,Math.max(.45,1.1+z*1.2),0,Math.PI*2);ctx.fill()});ctx.strokeStyle="rgba(131,203,255,.08)";ctx.lineWidth=1;for(let i=-2;i<=2;i++){ctx.beginPath();ctx.ellipse(cx,cy,r,r*Math.max(.12,.32+Math.abs(i)*.13),0,0,Math.PI*2);ctx.stroke()}rot+=.0018+(target-rot)*.002;target*=.985;requestAnimationFrame(draw)}draw();
return{setTarget:v=>target=v}}
const mainGlobe=earth?globe(earth,1500):null;const miniGlobe=worldCanvas?globe(worldCanvas,700):null;
addEventListener("scroll",()=>{const y=scrollY;earth&&(earth.style.transform="translateY("+Math.min(y*.08,70)+"px)");if(mainGlobe)mainGlobe.setTarget(y*.00035)});
